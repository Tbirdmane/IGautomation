# Core renderer
Pillow>=10.0

# Optional — generate.py / ideas.py (topic & research via the Claude API)
# anthropic>=0.40

# Optional — only for the OpenAI image provider in imagegen.py.
# Nano Banana (the default image provider) uses plain REST and needs NO package.
# openai>=1.0
